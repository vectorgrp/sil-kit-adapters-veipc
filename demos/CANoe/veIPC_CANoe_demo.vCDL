version 1.4;

import module "SilKit";

namespace Namespace
{ 
  [Binding="SilKit"]
  interface AdapterIf
  {
    provided data list<uint8> toSocket;
    consumed data list<uint8> fromSocket;
  }
  
  object Adapter : AdapterIf;
}
